{
  // Connection strings for the application, specifying the database connection details
  "ConnectionStrings": {
    "DefaultConnection": "Server=lostandfound.cxgu4eeosuet.us-east-1.rds.amazonaws.com;Database=lostandfounddb;User Id=admin;Password=Admin1234!;TrustServerCertificate=True;"//depricated, change to yours
  },

  // Logging configuration for the application
  "Logging": {
    "LogLevel": {
      // Default logging level for the application
      "Default": "Information",
      // Logging level specifically for ASP.NET Core
      "Microsoft.AspNetCore": "Warning"
    }
  },

  // Hosts that are allowed to access the application
  "AllowedHosts": "*",

  // JWT (JSON Web Token) configuration settings
  "Jwt": {
    // Secret key used to sign the JWT tokens
    "Key": "d1113290602d90b98b02c0155e88f3510b95798ebd1e8c4d66a0d6d04f77ae53"
  },

  // SendGrid settings for email sending
  "SendGridSettings": {
    // Email address used as the sender
    "SenderEmail": "lostandfoundsheridan@gmail.com",
    // Name associated with the sender email
    "SenderName": "Lost and Found team"
  }
}
